{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "cd urlshortener && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn urlshortener.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
  }
}
